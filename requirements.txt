# FastAPI 웹 프레임워크
fastapi>=0.116.1

# ASGI 서버
uvicorn[standard]>=0.35.0

# 데이터 검증 및 직렬화 (FastAPI와 함께 사용)
pydantic>=2.0.0

# HTTP 클라이언트 라이브러리
requests>=2.31.0

# 이미지 처리 라이브러리
Pillow>=10.0.0

# HTTP 라이브러리 (urllib3 버전 충돌 해결)
urllib3>=2.0.0

# 환경변수 관리
python-dotenv>=1.0.0  # .env 파일 로딩을 위한 경우

# 추가 유용한 패키지들 (선택사항)
python-multipart>=0.0.6  # 파일 업로드를 위한 경우
python-jose[cryptography]>=3.3.0  # JWT 토큰 처리를 위한 경우
passlib[bcrypt]>=1.7.4  # 비밀번호 해싱을 위한 경우
sqlalchemy>=2.0.0  # 데이터베이스 ORM을 위한 경우
alembic>=1.12.0  # 데이터베이스 마이그레이션을 위한 경우
